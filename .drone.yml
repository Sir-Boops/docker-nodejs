pipeline:
    docker:
        image: plugins/docker
        dockerfile: Dockerfile
        repo: sirboops/nodejs
        tags: 8.16.0-alpine
        experimental: true
        squash: true
        secrets: [ docker_username, docker_password ]
